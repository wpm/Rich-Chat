{
  "name": "rich-chat-e2e",
  "private": true,
  "description": "Drives the built app in headless Chromium and checks what renders.",
  "type": "module",
  "scripts": {
    "test": "node check.mjs ../dist"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
